# 今日优化总结（2026-09-09）

本次会话围绕两条主线：① 让应用/模型配置更方便迁移和复用；② 排查并修复高级模式在特定图片上耗时异常（最长达 16 分钟）的问题。

---

## 一、应用配置与模型下载位置

**问题背景**：`.env.example` 一直只是个"看得见但从没被读取"的模板文件，改它没有任何效果；同时 Qwen3-VL 模型默认下载到用户目录 `~/.cache/huggingface`，跟应用代码不在一起，不方便整体迁移。

**改动**：
- 新增 `python-dotenv` 依赖，`image_insight/config.py` 启动时自动加载项目根目录下的 `.env`（不存在也不报错），不再需要手动 `export`/`setx`。
- `HF_HOME` 默认值改为 `<项目根目录>/models/huggingface`（仅在没人显式设置时生效），模型和代码从此在同一个可移动的文件夹里。
- `QWEN_MODEL_ID` 支持直接填本地文件夹路径（不只是 HuggingFace 仓库名），方便复用同事已下载好的模型，不触发重新下载。
- 同步更新 `.env.example`、`README.md`、`README.zh.md`、`.gitignore`（排除新的 `models/huggingface/` 目录）。

**涉及文件**：`image_insight/config.py`、`.env.example`、`requirements.txt`、`.gitignore`、`README.md`、`README.zh.md`

**提交记录**：
- `d056fb7 feat: load config from .env and default model download into app root`
- `c8509c5 docs: sync README.zh.md with the .env/HF_HOME notes added to README.md`

---

## 二、高级模式异常耗时问题（核心）

### 现象
上传 `图片/10.png`（一张 3072×4096 的茅台套装照片）跑高级模式，**耗时 981.9 秒（16 分 22 秒）**，且最终仍解析失败。

### 排查过程与根因

1. **第一层原因**：`QWEN_MAX_NEW_TOKENS`（高级/快速模式共用）原本是 `99999`——这个值一直写在 `.env.example` 里，但因为 `.env` 从未被真正加载，从未生效过；本次加上 `.env` 自动加载后，这个沉睡多年的超大值才第一次真正生效，导致单次生成最多可以跑到近 10 万 token。
2. **第二层原因（真正的根因）**：把 token 上限降到合理范围后，问题依然存在。抓取模型原始输出后发现，**模型在 `visible_text` 字段里陷入了精确重复循环**——把"MOUTAI"、"防伪追溯器方法介绍"、"贵州茅台酒股份有限公司"这三句话循环写了约 90 遍，JSON 全程无法闭合，直到耗尽 token 上限。裤子/上衣等文字少的照片完全不会触发这个问题，说明是**图片里文字密度触发了模型的复读循环**，不是分辨率本身的问题（三张图缩放后分辨率相近，且原有的 `max_pixels` 早就把视觉 token 限制在约 1MP，缩放主要是省一点解码/编码开销）。

### 已实施的修复（仅限高级模式，快速模式零改动）

| 修复项 | 内容 |
|---|---|
| 大图预缩放 | 图片总像素 > 4,000,000 时，等比例缩放到最长边 2048（内存中处理，不覆盖原图，不影响 OCR 读取原图） |
| 独立生成上限 | 新增 `QWEN_ADVANCED_MAX_NEW_TOKENS=4096`，与快速模式的 `QWEN_MAX_NEW_TOKENS=9999` 彻底解耦 |
| 真超时保护 | 新增 `QWEN_ADVANCED_TIMEOUT=300`（秒），用 `StoppingCriteria` 在 `model.generate()` 内部按 token 粒度检查截止时间——**真正打断生成、释放显存**，不是只在 HTTP 层放弃等待而底层还在空转 |
| Prompt 约束 | 明确要求"JSON 闭合后立即停止，不要输出解释/推理过程，不要重复同一物品或字段" |
| `visible_text` 限制 | Prompt 要求最多 8 条、每条 ≤15 字、禁止重复；解析成功后再做一次去重+截断兜底（双保险） |
| 性能日志 | 新增 `[AdvancedVision]` 日志行，记录原始/处理后尺寸、token 数、推理耗时、`finish_reason`、是否超时等 |

### 踩过的坑（已回退，如实记录）

为了更彻底地根治"复读循环"，先后尝试过两种更激进的解码层手段，**都被证实会把裤子/上衣这种正常照片的输出搞成乱码**（出现无效的 Unicode 代理对字符），已全部回退：

- `transformers` 的 `no_repeat_ngram_size`（硬阻断重复片段）：阈值低了挡不住循环，阈值高了直接让模型输出乱码。
- 把 `repetition_penalty` 从 1.15 调到 1.3：同样会在正常照片上产生乱码。

**结论**：这个 2B 模型 + 贪心解码的组合对硬性重复限制非常敏感，最终没有依赖任何解码参数微调，`repetition_penalty` 保持和快速模式一致的 1.15。真正起效的是"更低的 token 上限 + 图片预缩放 + prompt 约束 + 300 秒兜底超时"这个组合拳。

### 修复效果实测（同一张 `图片/10.png`，多轮重复测试）

| 阶段 | 耗时 | completion_tokens | finish_reason |
|---|---|---|---|
| 原始状态（问题复现） | 981.9 秒 | 打满 9999 上限 | length |
| 加缩放 + 4096 cap + prompt 约束 | 165.9 秒 | 打满 4096 上限 | length |
| 最终版本（4 次重复测试） | 9.7s / 18.4s / 22.5s / 165.0s | 256~988 不等 | 均为 stop |

耗时存在波动（已确认是 GPU 浮点运算非确定性导致贪心解码在同一输入下生成长度不同，与代码无关），**最坏情况约 166 秒，稳定落在新设的 300 秒超时线以内**；对照组裤子图/上衣图全程稳定在 6~8 秒、finish_reason 均为 stop。

### 已知遗留问题（本轮未处理，供后续参考）

测试过程中额外发现：这个 2B 模型偶尔会对同一张普通照片生成**结构合法但内容是乱码**的字段（有效 JSON，但字符串本身是无效字符），大概率也是 GPU 浮点非确定性相关，**是模型本身早就存在的可靠性问题，与本次卡顿修复无关**，只是这次高强度重复测试才暴露出来。目前系统会靠 `needs_manual_review` 兜底转人工复核，但无法自动判断"内容是乱码"这件事本身。如果要处理，可以考虑：
- 加一层输出内容基本合法性检测（比如乱码字符检测）；
- 或者评估换用参数量更大、更稳定的模型（如 4B）。

**涉及文件**：`image_insight/config.py`、`image_insight/vlm/qwen_client.py`、`image_insight/api/main.py`、`.env`、`.env.example`、`tests/test_qwen_client.py`

**测试**：全部 154 个测试通过（147 个原有 + 新增 8 个高级模式专属用例）；`fast_analyzer.py`、`test_fast_analyzer.py`、快速模式 API/Prompt/参数全程未改动。

**提交状态**：这部分改动目前还**未提交**到 git（`git status` 显示为未暂存的修改），需要的话可以随时帮你提交推送。

---

## 三、本机 GPU/环境信息（顺带核实）

- 显卡：NVIDIA GeForce RTX 3070 Ti，8GB 显存，Compute Capability 8.6
- 驱动：596.21（支持 CUDA 13.2）；系统另装的 CUDA Toolkit（nvcc）：12.8
- PyTorch：2.6.0+cu124（自带 CUDA 12.4、cuDNN 9.1.0）
- transformers 4.57.6 / accelerate 1.10.1 / paddlepaddle-gpu 3.3.1 / paddleocr 3.7.0
- 已知问题：PaddleOCR 初始化时因 `cudnn_cnn64_9.dll` 加载失败而禁用（torch 和 paddle 各自打包的 cuDNN 版本/来源没对上），有容错，不影响主流程，高级模式的可见文字交叉校验功能处于关闭状态。
