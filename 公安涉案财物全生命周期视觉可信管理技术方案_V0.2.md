# 公安涉案财物全生命周期视觉可信管理技术方案 V0.2

> 文件状态：内部讨论稿  
> 日期：2026年8月28日  
> 用途：支撑“一物一视觉档案、关键节点辅助核验、异常人工闭环”的产品建设  
> 核心原则：模型可替换、结果可解释、专业边界可控制、业务责任不改变。

## 1. 技术目标

本方案服务于《公安涉案财物“一物一档”视觉可信管理产品需求与必要性分析 V0.2》。技术体系不以单一模型覆盖市局29+1全部类型为目标，而是在公安内网中稳定完成以下闭环：

> 图像质量控制—物品定位—适用类别识别—OCR可见文字—业务类型映射—人工确认—视觉建档—节点比较—差异复核—全程留痕。

V1首先完成首次登记建档，并在接收或入库节点验证一次二次采集和辅助比较。后续再扩展至借出、归还、移交和处置。

## 2. 总体技术原则

1. **业务类型与视觉类别解耦**：市局业务字典保持稳定，视觉模型只识别外观上可学习的目标，通过映射和人工确认进入正式类型。
2. **检测、OCR与比较能力解耦**：检测回答“看到了什么、在哪里”，OCR回答“物品上写了什么”，视觉档案比较回答“前后有哪些相同点和明显差异”。
3. **建议与事实分离**：接口明确区分可观察事实、业务类型建议、差异提示和人工最终结论。
4. **Unknown是一等能力**：低置信度、质量不合格或不在覆盖范围的对象必须合理回退人工。
5. **专业认定始终由人工完成**：毒品、枪支、管制刀具、假币等不得由普通视觉模型自动定性。
6. **原始数据不可覆盖**：原图、衍生图、AI预测、OCR结果、人工确认和模型/规则版本分别保存。
7. **业务系统松耦合**：现有涉案财物系统继续管理主数据和法定流程，视觉系统通过统一接口提供辅助能力。

## 3. 总体架构

| 层级 | 主要组件 | 核心职责 |
|---|---|---|
| 采集层 | 顶置相机、补光、采集服务、质量门禁 | 生成规范原图，支持翻面和局部补拍，标记采集阶段 |
| 视觉能力层 | 检测模型、OCR、独立Embedding模型 | 物品定位、视觉类别、可见文字和长期视觉特征 |
| 证据与比较层 | 类型映射、Evidence Fusion、Unknown、Compare Engine | 生成可解释建议，控制专业边界，比较不同节点的明显差异 |
| 视觉档案层 | Visual Dossier Service、图像索引、节点时间轴 | 按goods_id组织首次登记及后续节点的视觉记录 |
| 业务服务层 | Vision Gateway、Model Adapter、复核页面、业务接口 | 统一API、人工确认、差异复核和业务回填 |
| 数据审计层 | PostgreSQL、pgvector、NAS/对象存储、审计日志 | 保存图片、元数据、模型结果、人工真值、版本及操作记录 |

## 4. 采集工位与图像质量

图像采集是产品核心能力，不是模型前的附属步骤。V1标准工位建议包含固定相机、稳定补光、低纹理统一背景和受控拍摄区域。

质量门禁至少检查模糊、过曝、欠曝、目标越界、严重遮挡和多件叠放。单张图无法反映品牌、型号文字、编号或关键外观时，系统应提示翻面或局部补拍。

每张图必须记录goods_id、capture_stage、image_role、拍摄时间、设备标识和操作人员。capture_stage至少预留entry、receive、storage、lend、return、transfer、disposal。

## 5. 市局29+1类型映射策略

业务层继续使用市局正式类型；模型层另建visual_target和识别策略，两者通过category_mapping关联。

| 技术策略 | 适用对象 | 实现方式 |
|---|---|---|
| 视觉重点 | 手机、电瓶、首饰、钱包、电脑等 | 目标检测/分类，OCR补充品牌和型号文字 |
| 文档卡片 | 银行卡、储值卡、发票、合同、公文、票据等 | 文档或卡片定位、OCR、规则映射、人工确认 |
| 大件物品 | 自行车、电动自行车、机动车 | 后续适配移动或现场拍摄，不作为桌面工位核心 |
| 非实体对象 | 账户、房产等 | 依赖业务数据和人工选择 |
| 专业认定对象 | 枪支、毒品、管制刀具、假币等 | 仅输出可观察事实或外观候选，强制人工确认 |
| 未知对象 | 未覆盖或低置信度物品 | 返回Unknown并进入人工选择 |

正式研发前必须取得市局当前有效的类型编码表，确认名称、编码、状态和“种毒”等待核定义。

## 6. 视觉识别与OCR

V1可继续将RF-DETR-S/M作为目标检测候选，将PP-OCRv5作为OCR候选，但两者必须通过Model Adapter接入，业务接口不得绑定具体框架、GPU或模型品牌。

默认采用Detection First、OCR Second：先定位物品区域，再对Crop进行矫正、放大和OCR，减少背景文字干扰。不同类型配置不同OCR Profile，重点提取品牌、型号文字、SN/IMEI、容量、规格、编号、标题和文种等可见线索。

OCR结果属于可见证据，不直接等同于正式字段。银行卡号、IMEI等敏感内容应按现有权限显示、脱敏和审计。

## 7. Evidence Fusion与专业边界

Evidence Fusion融合视觉类别、OCR、业务字典、映射规则和风险策略，输出“建议＋依据”，而不是单一类别字符串。

建议输出至少包括：visual_class、visual_confidence、ocr_evidence、observed_facts、suggested_business_type、risk_policy、requires_manual_confirmation、model_version和rule_version。

当置信度不足、候选差距过小、图像质量不合格或对象不在覆盖范围时返回Unknown。对枪支、毒品、管制刀具、假币等类型强制requires_manual_confirmation=true，禁止接口自动提交专业结论。

## 8. 单件视觉档案

### 8.1 档案组织

goods_id使用现有业务系统的财物唯一标识。同一件财物在不同节点形成多组原图、Crop、OCR、视觉特征和人工确认结果，由Visual Dossier Service按时间轴组织。

### 8.2 两类数据资产

- 类别样本与特征库服务于“这种物品是什么”，用于训练、评估和难例管理。
- 单件视觉档案服务于“这件物品前后是否存在需要关注的变化”，用于节点辅助核验。

两类资产必须分开设计，避免把业务档案直接当作未经治理的训练数据。

## 9. 节点辅助比较

Compare Engine接收同一goods_id在两个业务节点的图像及结构化信息，辅助比较：

- 视觉大类是否一致；
- 品牌、型号文字、编号等可见信息是否一致或缺失；
- 数量、包装、颜色和明显外观是否发生变化；
- 当前图像质量是否足以完成核验。

比较结果应展示前后原图、关键区域、相同点、差异点和置信信息。系统只能生成“需要关注的差异”，不得直接认定物品被调换、损坏或责任归属。

V1不以复杂细粒度同一性识别为验收前提。首期重点验证标准采集条件下，明显差异能否被组织、提示并进入人工闭环。

## 10. 人工复核与异常闭环

复核页面支持查看原图、局部图、OCR、视觉建议和节点比较结果。业务人员可以确认正常、要求补拍、修正类型、补充说明或转入整改。

系统应记录复核人员、时间、意见、最终类型、修正原因和处理结果。AI原始输出不得被人工结果覆盖，确保后续能够复盘模型效果和操作责任。

## 11. 数据模型

| 对象 | 关键字段/说明 |
|---|---|
| business_category | city_type_code、city_type_name、active，保存正式业务字典 |
| category_mapping | 业务类型、visual_target、strategy、ocr_profile、manual_required、status |
| goods_image | goods_id、capture_stage、image_role、original_uri、sha256、时间、设备、操作人 |
| vision_detection | image_id、crop_id、模型版本、视觉类别、置信度、bbox、推理耗时 |
| ocr_result | crop_id、OCR版本、文本、置信度、位置、字段类型、脱敏文本 |
| vision_decision | 建议业务类型、可观察事实、证据、风险策略、是否强制人工确认 |
| human_confirmation | AI建议、最终类型、操作人、确认时间、修正原因 |
| vision_embedding | crop_id、模型版本、维度、向量、生成时间 |
| visual_comparison | goods_id、基准图、当前图、比较版本、相同点、差异点、风险等级 |
| review_record | 比较记录、复核结论、说明、处理动作、复核人及时间 |
| model_registry | 模型、数据集、规则版本、阈值、部署时间和回滚信息 |

## 12. 服务接口

| 接口 | 职责 |
|---|---|
| POST /vision/capture/check | 图像质量检查和重拍/补拍建议 |
| POST /vision/analyze | 检测、OCR、融合，返回建议及依据 |
| POST /vision/confirm | 接收人工确认或修正，形成正式标签闭环 |
| POST /vision/images | 保存不同节点和角色的原图、补拍图 |
| GET /vision/dossier/{goods_id} | 查询单件财物视觉时间轴 |
| POST /vision/compare | 对同一财物两个节点进行辅助比较 |
| POST /vision/review | 保存人工复核、说明和处理结果 |
| GET /vision/categories | 查询业务类型映射和可用策略 |
| GET /vision/models | 查询当前模型、数据集和规则版本 |

## 13. 存储、安全与审计

- PostgreSQL保存业务映射、AI/OCR元数据、人工真值、比较结果、规则版本和关联关系。
- 原图、Crop和必要衍生图保存在公安内网NAS或对象存储，数据库保存URI、哈希和元数据。
- 原始图片保存SHA-256；原图与增强图、标注图分开存储。
- 原则上在公安内网或符合项目要求的专网私有部署，不调用公网第三方推理服务。
- 查询、导出、复核和模型变更均进入审计日志；敏感OCR内容执行权限控制和脱敏。
- 视觉相似检索、跨案件或跨单位使用必须单独论证用途、权限和审计要求。

## 14. 训练数据与模型生命周期

训练标注先定义可由外观稳定学习的visual_target，再通过category_mapping映射业务类型。数据集按物品实例划分训练、验证和测试，避免同一件物品的不同照片跨集合造成数据泄漏。

样本应覆盖标准图、翻面图、局部图、反光、遮挡、包装、破损和相似物等难例。上线后人工修正进入错误池，但必须经过数据审核后才能进入新训练集。

dataset_version、model_version和rule_version分别管理。新模型必须在冻结测试集、难例集和真实现场抽样集上通过回归评估后上线，并保留回滚能力。

## 15. V1实施路径

| 阶段 | 工作内容 | 主要输出 |
|---|---|---|
| A 业务口径 | 获取正式类型编码，确认高频类别、业务字段和试点节点 | 类别字典、适用性矩阵、字段映射 |
| B 工位与样本 | 搭建标准顶拍工位，采集代表性样本和难例 | 采集规范、初始数据集、人工真值 |
| C 能力验证 | 对检测、OCR、Unknown和专业边界进行分项测试 | 模型/OCR/安全回退评估报告 |
| D 建档闭环 | 建设Vision API、确认页面、业务回填和视觉档案 | 可运行首次登记建档MVP |
| E 节点试点 | 在接收或入库节点进行二次采集和辅助比较 | 节点核验试点评估、差异样本 |
| F 运行闭环 | 建立复核、说明、错误池和版本发布机制 | 运行规范、验收报告、迭代计划 |

## 16. 评估与验收

验收同时覆盖模型、产品、业务和审计：

- 适用类别按类统计Top-1准确率、Top-3覆盖率、Precision和Recall。
- Unknown回退合理率、误强判率及高风险类型自动专业认定数量。
- 品牌、型号文字、编号、标题等OCR有效提取率。
- 一次拍摄通过率、补拍率和单件辅助录入耗时。
- 视觉档案完整率、关键节点二次采集率和版本可还原率。
- 差异提示进入人工复核的完成率、闭环率和材料形成时间。

具体数值阈值应在真实样本基线测试后确认。任何AI能力必须分步骤形成“达标、不达标、待补样本”结论，不使用模糊评价。

## 17. 后续演进

- V2：将节点核验扩展至借出、归还、移交和处置，形成完整视觉时间轴。
- V3：建设相似物品检索、品牌/型号细粒度识别及差异规则库。
- V4：形成单位、节点、异常和闭环情况的管理分析能力。
- V5：在权限、用途和正式建设边界明确后，探索跨单位视觉数据治理。

## 附录：技术选型建议

| 模块 | V0.2候选 | 说明 |
|---|---|---|
| 目标检测 | RF-DETR-S/M或可替换同类模型 | 在公安真实数据和目标硬件上A/B测试后定型 |
| OCR | PP-OCRv5或可私有部署同类OCR | 中文、英文、数字、型号和文档标题分项验证 |
| Embedding | 独立可插拔视觉Encoder | 避免检测模型升级破坏长期特征体系 |
| 证据融合 | 规则、字典、正则和策略引擎 | V1强调确定性、可解释，不依赖大模型主链路 |
| 服务 | FastAPI或等价统一API服务 | 通过Model Adapter隔离模型与硬件差异 |
| 数据库 | PostgreSQL＋pgvector | 业务关联、元数据及初期向量能力 |
| 图片 | 公安内网NAS或对象存储 | 原图和衍生图分开保存 |

