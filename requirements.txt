# Web app
fastapi
uvicorn
python-multipart
jinja2
pydantic
python-dotenv

# Fast mode (RF-DETR pretrained detection)
# Install torch/torchvision FIRST from the CUDA index for GPU support, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
torch
torchvision
rfdetr

# Advanced mode (Qwen3-VL, runs fully locally — no API key/network at request time)
transformers>=4.57.0
accelerate
qwen-vl-utils
pillow

# Advanced mode OCR pass
# Install paddlepaddle-gpu FIRST from the official index, e.g.:
#   pip install paddlepaddle-gpu -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
paddlepaddle-gpu
paddleocr

# Remote GPU backend (optional — QWEN_BACKEND=remote, see .env.example).
# Only this is needed for the Qwen3-VL call itself when running on a
# GPU-less host that talks to a remote OpenAI-compatible endpoint
# (vLLM/SGLang/Xinference) instead: torch/transformers/accelerate/
# qwen-vl-utils above and paddlepaddle-gpu (GPU build) are only required for
# the default QWEN_BACKEND=local path and OCR respectively — skip them (or
# swap paddlepaddle-gpu for the CPU-only `paddlepaddle` package) on a
# machine that only ever runs the remote backend.
requests

# Tests
pytest
pytest-asyncio
